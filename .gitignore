.Rproj.user
.Rhistory
.RData
.Ruserdata

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Local design scratch file, not part of the published site
mockup.html

# SvelteKit source of site and decks lives in deck/; only its export (root, slides/, _app/) is published
deck/node_modules/
deck/build/
deck/.svelte-kit/
